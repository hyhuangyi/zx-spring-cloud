spring:
  application:
    name: zx-provider
  profiles:
    active: dev
server:
  port: 8002
logging:
  config:  classpath:logback-spring.xml

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
  instance:
    prefer-ip-address: true
